{
  "name": "agent-flow-runtime",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Observable multi-agent workflow runtime — a reusable visible-run tracer over Herdr.",
  "module": "src/index.ts",
  "scripts": {
    "smoke": "bun run src/smoke/run-smoke.ts",
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "typescript": "^7.0.2"
  }
}
